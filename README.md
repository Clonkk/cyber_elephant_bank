## README

Install uv: https://docs.astral.sh/uv/getting-started/installation/

Get deps : `uv sync`

Run server: `uv run main.py`
Clear local db and run server: `uv run main.py --clear 1`

Generate pip compatible deps: `uv pip freeze > requirements.txt`


